name: update changelog

on:
  release:
    types: [released]

permissions: {}

jobs:
  update:
    permissions:
      contents: write
    uses: shop/.github/.github/workflows/update-changelog.yml@main
